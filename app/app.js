window.addEventListener("load", function() {
  //test -----
  CSSDeviceManager.start();
  /*
  var led = document.getElementById("led");
  styleObserver.observe(led, styleObserverConfig);
  led.style.color = "rgba(200,200,200, 0.1)";
  var newElement = document.createElement("lable");
  newElement.textContent = "label";
  newElement.setAttribute("type", "led");
  document.body.appendChild(newElement);
  */
});
